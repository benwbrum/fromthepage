#reasoning.ai-reasoning
  -if @ai_transcription&.supports_reasoning? && @ai_transcription&.reasoning&.present?
    h4 =t('.ai_reasoning')
    p.reasoning-explanation =t('.reasoning_explanation')
    .reasoning-content
      == Markdown.new(@ai_transcription.reasoning).to_html
