<%= logger.debug("in view/display/read_work.html.erb, work.id is #{@work.id}") %>
<%= render({ :partial => '/shared/work_tabs',
             :locals => { :selected =>1,
                          :work_id => @work.id }}) %>


<%= render({ :partial => 'multi_page'}) %>
