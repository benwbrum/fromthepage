<p>
    Pages for <%= @work.title %>:
</p>

<p>
  <% for page in @work.pages %>
    <%= link_to(page.title,
                { :action => 'display_page',
                  :page_id => page.id } ) %>

    <br />
  <% end %>
</p>
