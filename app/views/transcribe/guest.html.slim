=render({ :partial => '/shared/page_tabs', :locals => { :selected => 3, :page_id => @page.id }})

=link_to "Transcribe as guest", { :controller => 'application', :action => 'guest_transcription', :page_id => @page.id}
