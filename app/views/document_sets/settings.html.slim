=render({ :partial => '/shared/collection_tabs', :locals => { :selected => 8, :collection_id => @collection.id }})
-link = link_to 'here', document_sets_path(:collection_id => @collection.collection)
p.big This is a document set based on the #{@collection.collection.title} collection.  You can manage all works and document sets within that collection by clicking #{link}.

=render 'edit'