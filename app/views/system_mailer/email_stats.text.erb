Stats on FromThePage

Hours: <%= @hours %>

User Stats for FromThePage

These are the new users from the last <%= @hours %> hours.

There are <%= @recent_users.count %> new users in the last <%= @hours %> hours.

<% @recent_users.each do |user| %>
<%= link_to user.display_name, { :controller => 'user', :action => 'profile', :user_id => user.id, only_path: false} %>
<%= user.email %><br>
<% end %>

