-selected_tab = tabs.find { |tab| tab[:selected] == selected }[:name] rescue ""
-content_for :page_title, "#{selected_tab} - #{t('.administration')}"

h1 =t('.administration')
.tabs
  =active_link_to t('.summary'), admin_path, active: :exclusive
  =active_link_to t('.owners'), admin_owner_list_path
  =active_link_to t('.users'), admin_user_list_path
  =active_link_to t('.abuse'), admin_flag_list_path
  =active_link_to t('.uploads'), admin_uploads_path
  =active_link_to t('.logfile'), admin_tail_logfile_path
  =active_link_to t('.settings'), admin_settings_path
