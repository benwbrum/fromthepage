=form_tag(url, method: :get, id: selector, data: dataset) do
  -if sorting.present?
    =hidden_field_tag :sort, sorting
  -if ordering.present?
    =hidden_field_tag :order, ordering

  -static_params.each do |key, value|
    =hidden_field_tag key, value

  =yield
