=render({ :partial => '/shared/collection_tabs', :locals => { :selected => 5, :collection_id => @collection }})
=render({ :partial => 'edit_tabs', :locals => { :selected => 2}})

.collection-settings-wrapper
  =form_for :collection, url: collection_update_path(@collection) do |f|
    =validation_summary @collection.errors
    table.form.collection-settings
      tr
        td(colspan="2")
          h3(style="display:inline-block") =f.label :text_language, t('.text_language'), class: 'collection-label'
          =f.select :text_language, options_for_select(@text_languages, (@collection.text_language ? @collection.text_language : 'eng')), {}, {class: 'w50', style: 'display: inline-block'}
          p =t('.text_language_description')   
      tr 
        td(colspan="2")
          h3 =t('.transcription_type')
          p =t('.transcription_type_description')
      tr 
        td(colspan="2")
          =f.radio_button :field_based, false, :checked => !@collection.field_based
          =f.label :field_based
            h3 =t('.document_based_transcription')
            p =t('.document_based_transcription_description')
          =link_to t('.edit_buttons'), collection_editor_button_edit_path(:collection_id => @collection.id), disabled: @collection.field_based, class: 'button', id: 'document-based-edit', 'aria-label' => t('.edit_buttons'), :data => { litebox: { hash: 'edit-buttons' }}          
      tr 
        td(colspan="2")
          =f.radio_button :field_based, true, :checked => @collection.field_based
          =f.label :field_based
            h3 =t('.field_based_transcription')
            p =t('.field_based_transcription_description')
          =link_to t('.edit_fields'), transcription_field_edit_fields_path(:collection_id => @collection), disabled: !@collection.field_based, class: 'button fields', id: 'field-based-edit', 'aria-label' => t('.edit_fields')
      
      tr
        td(colspan="2")
          =f.check_box :data_entry_type, :checked => @collection.metadata_entry?
          =f.label :data_entry_type
            h3 =t('.enable_metadata_description')
            p =t('.enable_metadata_description_description')
          =link_to t('.edit_metadata_fields'), transcription_field_edit_metadata_fields_path(:collection_id => @collection), disabled: !@collection.metadata_entry?, class: 'button fields', id: 'metadata-fields-edit', 'aria-label' => t('.edit_fields')    
      tr 
        td(colspan="2")
          =f.check_box :subjects_enabled
          =f.label :subjects_enabled
            h3 =t('.subjects_enabled')
            p =t('.subjects_enabled_description')
      tr 
        td(colspan="2") 
          -if @collection.works.ocr_disabled.any?
            =link_to(collection_enable_ocr_path(:collection_id => @collection.id), class: 'button')
              span =t('.enable_ocr')
          -if @collection.works.ocr_enabled.any?
            =link_to(collection_disable_ocr_path(:collection_id => @collection.id), class: 'button')
              span =t('.disable_ocr')
          p =t('.ocr_description')
      tr 
        td: td: td
          .toolbar
            .toolbar_group
              =f.button t('.save_changes'), class: 'big', id: 'collection_settings_save'

-content_for :javascript
  javascript: 
    $(document).ready(function() {
       $('input[type=radio][name="collection[field_based]"]').change(function() {
        if ($(this).val() == 'true') {
          $('#field-based-edit').removeAttr('disabled');
          $('#document-based-edit').attr('disabled', 'disabled');
        } else {
          $('#field-based-edit').attr('disabled', 'disabled');
          $('#document-based-edit').removeAttr('disabled');
        }
      });
      $('input[type=checkbox][name="collection[data_entry_type]"]').change(function() {
        if ($(this).is(':checked')) {
          $('#metadata-fields-edit').removeAttr('disabled');
        } else {
          $('#metadata-fields-edit').attr('disabled', 'disabled');
        }
      });
    });
