Hello <%= @owner.display_name %>,

A user has pasted text into the transcription editor for your collection "<%= @collection.title %>". This may indicate they are using AI assistance (like ChatGPT) instead of transcribing from the page image.

User: <%= @user.display_name %> (<%= @user.email %>)
Collection: <%= @collection.title %>

Please review their work to determine if it's appropriate. You can:
- View all of their contributions to this collection
- Approve them if their use of paste is legitimate (e.g., transcribing in a separate document)
- Contact them to discuss proper transcription methods
- Block them from the project if necessary

View user contributions: <%= collection_user_contribution_list_url(@collection.owner, @collection, @user.slug, only_path: false) %>

Manage suspicious behaviors: <%= dashboard_suspicious_behaviors_url(only_path: false) %>

Note: If you approve this user, you will not receive further notifications about their paste activity.

Best regards,
The FromThePage Team
