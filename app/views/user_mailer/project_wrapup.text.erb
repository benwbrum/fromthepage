Hello <%= @wrapup.owner.display_name %>,

Wow! <%= @wrapup.title %> project is 100% transcribed! What's next?
- Send a wrap up email to all of your collaborators announcing they did it! Remind them of the purpose of the project, thank them for their work, and direct them on to your next project or a colleague's project.

- Export your transcriptions if you'd like. You can export your transcriptions in a variety of formats, <%= link_to "here", collection_export_url(@wrapup.owner, @wrapup.collection, only_path: false), target: '_blank' %>. Or you can use the <%= link_to "IIIF API", IIIF_API_WIKI_LINK, target: '_blank', rel: 'nofollow' %>. If you need custom integration services to pull them into a downstream system, we can help - send us an email at <%= mail_to SENDING_EMAIL_ADDRESS %>.

- You can also leave your project up on FromThePage - it has great Google SEO. Our perspective is that a transcription is never done. Someone may read something years from now and be able to correct a confusing point or add more context to your project.
- How'd your project go? We'd love to hear about the hardest part, the easiest part, and one (or more!) things we can improve in FromThePage.
- Start a new project!

Here are the statistics on your project:

- <%= number_with_delimiter @wrapup.work_count %> <%= 'Work'.pluralize(@wrapup.work_count) %>
- <%= number_with_delimiter @wrapup.page_count %> <%= 'Page'.pluralize(@wrapup.page_count) %>
- <%= number_with_delimiter @wrapup.comment_count %> <%= 'Note'.pluralize(@wrapup.comment_count) %>
- <%= number_with_delimiter @wrapup.contributor_count %> <%= 'Contributor'.pluralize(@wrapup.contributor_count) %>
- <%= number_with_delimiter @wrapup.transcription_count %> <%= 'Page'.pluralize(@wrapup.transcription_count) %> Transcribed
- <%= number_with_delimiter @wrapup.edit_count %> <%= 'Page'.pluralize(@wrapup.edit_count) %> Edited
- <%= number_with_delimiter @wrapup.translation_count %> <%= 'Page'.pluralize(@wrapup.translation_count) %> Translated
- <%= number_with_delimiter @wrapup.ocr_count %> OCR <%= 'Correction'.pluralize(@wrapup.ocr_count) %>

<% if @wrapup.subjects_enabled? %>
- <%= number_with_delimiter @wrapup.index_count %> <%= 'Page'.pluralize(@wrapup.index_count) %> Indexed
- <%= number_with_delimiter @wrapup.subject_count %> <%= 'Subject'.pluralize(@wrapup.subject_count) %>
- <%= number_with_delimiter @wrapup.mention_count %> <%= 'Reference'.pluralize(@wrapup.mention_count) %>
<% end %>

Here's a list of all your collaborator emails:
<%= @wrapup.contributor_emails %>

Sincerely,
The FromThePage Team
