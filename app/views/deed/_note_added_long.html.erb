<%= link_to(deed.user.display_name,
	                           { :controller => 'user',
	                               :action => 'profile',
	                               :user_id => deed.user.id }) %>
added a note to
<%= link_to(deed.page.title,
             { :controller => 'display',
               :action => 'display_page',
               :page_id => deed.page.id }) %>
saying
"<%= deed.note.title %>"
 <%= time_ago(deed.created_at) %>
