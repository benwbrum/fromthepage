h1 Activity Log
section.activity-log
  -for deed in @deeds
    .activity-log_record
      =time_tag(deed.created_at, class: 'activity-log_record_time')
        =time_ago_in_words(deed.created_at)
      .activity-log_record_text
        =render(:partial => 'deed/deed.html', :locals => { :deed => deed, :long_view => true })

p.aright
  =link_to 'Older Activity',
    { :offset => (@offset.to_i + 50),
      :collection_id => @collection ? @collection.id : nil,
      :user_id => @user ? @user.id : nil }
  |&nbsp;&rarr;