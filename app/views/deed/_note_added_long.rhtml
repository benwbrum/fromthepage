<%= deed.user.display_name %>
added a note to
<%= link_to(deed.page.title,
             { :controller => 'display',
               :action => 'display_page',
               :page_id => deed.page.id,
               :ol => 'd_act_page' }) %>
saying
"<%= deed.note.title %>"
 <%= time_ago(deed.created_at) %>