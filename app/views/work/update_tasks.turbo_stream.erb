<%= turbo_stream.update('collection-settings', partial: 'form', locals: { collection: @collection, work: @work, scope: 'edit_tasks' }) %>

<% if @result.success? %>
  <%= turbo_flash(t('work.update.work_updated'), :notice) %>
<% else %>
  <%= turbo_flash(t('errors.error'), :error) %>
<% end %>
