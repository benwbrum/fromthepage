
<div class="columns">

<article>
<p>
	Now it's time to import your page images into FromThePage.
</p>


<%= form_for(@document_upload) do |f| %>
  <% if @document_upload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document_upload.errors.count, "error") %> prohibited this document_upload from being saved:</h2>

      <ul>
      <% @document_upload.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :collection_id, "Collection to import into:" %><br>
    <%= f.select :collection_id, options_from_collection_for_select(current_user.collections, 'id', 'title'), prompt: '- Select a collection -' %>
  </div>
  <div class="field">
  	<p>
  		Upload a ZIP or PDF file containing page images to be used for your project. 
  	</p>
  	<ul>
  		<li>
  			ZIP files may contain folders containing images.
  		</li>  
  		<li>
  			Each folder will be treated as a different document, so do not mix pages from different documents in the same folder.
  		</li>
  		<li>
  			Each PDF will be treated as its own document, so do not split pages from the same document among more than one PDF.
  		</li>
  		<li>
  			ZIP files may contain PDFs
  		</li>
  	</ul>
    <%= f.label :file %><br>
    <%= f.file_field :file %>
  </div>
  <div class="actions">
    <%= f.submit 'Upload' %>
  </div>
<% end %>
</article>

<aside class="sidecol">
<p>
	Page image guidelines
</p>
<ul>
	<li>
		Images should be oriented so that they are right-side-up.
	</li>
	<li>
		PNG, GIF, and JPG are all acceptible.
	</li>
	<li>
		Images should be split down the spine, so that only one page is visible on an image.  
		(See the <a href="https://github.com/benwbrum/autosplit">autosplit</a> tool if you need to split two-page images.)
	</li>
	<li>
		Images should be named so that an alphabetical sort will result in the correct page order.  
		(This may require "zero-padding" for any page numbers:  <code>page_09.jpg, page_10.jpg</code> 
		will sort correctly, but <code>page_9.jpg, page_10.jpg</code> will not.)
	</li>
</ul>
</aside>
</div>
