<%= start_form_tag( {:action => 'number_location_process'} ) %>
    <p>Please click on the page number in this image.
    </p>
    <input type="image" src="<%= file_to_url(@image.shrunk_file) %>" name="coordinate" />

<%= end_form_tag %>