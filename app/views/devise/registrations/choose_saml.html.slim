-content_for :page_title, t('devise.sign_in')

section.signon
  h1= t('.sign_in_with_institution')
  =form_tag(registrations_set_provider_path) do
    fieldset.signon_fieldset
      =label_tag :institution, t('.institution'), class: 'signon_label'
        =select_tag(:institution, options_for_select([[t('.lds_full_name'), 'lds'],[t('.dartmouth_university'),'dartmouth'],[t('.harvard_university'),'harvard'],[t('.illinois_state'),'ilstu'],[t('.johns_hopkins_university'),'jhu'],[t('.nebraska_university'),'nebraska'],[t('.utah_id'),'utah']]), autofocus: true)
    .toolbar
      .toolbar_group
        =button_tag t('devise.sign_in'), class: 'strong signin'